namespace FreePolarAlign.App;

// Phase 0 stub. The real UI is an Avalonia application (README architecture
// diagram, D2); wiring Avalonia is deferred to Phase 4 since it is not
// load-bearing for any Phase 0 exit criterion. This console entry point only
// exists so the project builds and the solution layout is demonstrable.
public static class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("free-polar-align (Phase 0 skeleton). Avalonia UI arrives in Phase 4.");
    }
}
